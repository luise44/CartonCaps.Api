namespace CartonCaps.Transversal
{
    public enum ReferralChannel
    {
        SMS,
        EMAIL,
        APP
    }

    public enum AppChannelDetail
    {
        WHATSAPP,
        TELEGRAM,
        OTHER
    }

    public enum OnboardingForm
    {
        DEFAULT,
        REFERRED
    }
}
